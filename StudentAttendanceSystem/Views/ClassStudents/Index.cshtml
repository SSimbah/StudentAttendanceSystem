@model IEnumerable<StudentAttendanceSystem.Models.ClassStudent>

@{
    ViewData["Title"] = "Index";
}

<h1>Class List</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Class)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Class.ClassSubject)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student.FullName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ClassStudentID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ClassStudentID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ClassStudentID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
